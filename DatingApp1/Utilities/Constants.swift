import Foundation

enum Constants {
    enum Collections {
        static let users = "users"
        static let friendships = "friendships"
        static let friendRequests = "friend_requests"
    }
}
